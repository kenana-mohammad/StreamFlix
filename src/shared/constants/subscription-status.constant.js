const SUBSCRIPTION_STATUS = Object.freeze({
    ACTIVE: 'active',
    EXPIRED: 'expired',
    CANCELLED: 'cancelled',
    PENDING: 'pending',
    UPGRADED: 'upgraded'
});

module.exports = { SUBSCRIPTION_STATUS };